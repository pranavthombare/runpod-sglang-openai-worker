runpod>=1.3.0
requests>=2.31.0
# Needed by sglang at runtime when not installing extras
pybase64>=1.4.0
# If you need client-only utilities from sglang in worker-only image, you can also install:
# sglang
